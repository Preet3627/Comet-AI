// Browser-AI/comet-browser/src/app/settings/page.tsx
import SettingsPanel from '@/components/SettingsPanel';

export default function SettingsPage() {
  return (
    <SettingsPanel onClose={() => { /* Handle close logic if needed */ }} />
  );
}